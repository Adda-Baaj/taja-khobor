providers:
  - id: example
    name: Example Provider
    type: google_news_sitemap
    source_url: https://example.com/news-sitemap.xml
    response_format: xml
    request_delay_ms: 500
    config:
      user_agent: MyCrawler/1.0 (+https://example.com)
      accept: application/json
      accept_language: en-US,en;q=0.9


# Note: Do NOT commit actual credentials into this file. Keep secrets in env or secret manager.
